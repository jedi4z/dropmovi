{%extends "DropmoviFrontendBundle:Template:frontend_template.html.twig"%}
{%block title%} Editar perfil {{app.user.name ~" "~ app.user.lastName}} - {{parent()}}{%endblock%}
{%block container%}
<div class="container-fluid">
    <div class="span8">
        <form class="form-horizontal" method="POST" {{ form_enctype(form) }}>
            <div class="container-fluid">
                <legend>Editar Perfil</legend>
                <div class="well well-small">
                    Puedes cambiar la información que se mostrara en tu 
                    <a href="#" rel="tooltip" title="El perfil publico puede ser accedido desde internet por cualquier persona.">perfil publico</a>. 
                    Actualmente tu perfil publico esta en <a href="http://www.dropmovi.com/{{app.user.username}}">http://www.dropmovi.com/{{app.user.username}}</a>, 
                    recorda que lo puedes modificar en <a href="{{path ('dropmovi_frontend_configuration')}}">las configuraciones de la cuenta</a>.
                </div>
                <div class="span2">
                    <ul class="thumbnails">
                        <li class="span2">
                            <a href="{{app.user.getWebPath}}" target="blank" class="thumbnail">
                                <img src="{{app.user.getWebPath}}" alt="photo_user">                    
                            </a>                      
                        </li>              
                    </ul>
                </div>
                <div class="span5">
                    <div class="control-group">
                        <label class="control-label">Foto de perfil</label>
                        <div class="controls">
                            {{form_widget (form.file)}}
                        </div>
                    </div>
                    <div class="control-group">
                        <label class="control-label">Nombre</label>
                        <div class="controls">
                            {{form_widget (form.name)}}
                        </div>
                    </div>
                    <div class="control-group">
                        <label class="control-label">Apellido</label>
                        <div class="controls">
                            {{form_widget (form.lastName)}}
                        </div>
                    </div>
                    <div class="control-group">
                        <label class="control-label">Ubicacíon</label>
                        <div class="controls">
                            {{form_widget (form.location)}}
                        </div>
                    </div>
                    <div class="control-group">
                        <label class="control-label">Sitio web</label>
                        <div class="controls">
                            {{form_widget (form.website)}}
                        </div>
                    </div>
                    <div class="control-group">
                        <label class="control-label">Biografía</label>
                        <div class="controls">
                            {{form_widget (form.biography)}}
                        </div>
                    </div>
                </div>                         
            </div>
            <div class="form-actions">
              <button type="submit" class="btn btn-primary">Guardar cambios</button>
              <a type="button" class="btn" href="{{path ('dropmovi_frontend_profile')}}">Cancelar</a>
            </div>
            {{form_rest (form)}}
        </form>
    </div>
    <div class="span4">
        <!--Sidebar content-->
    </div>
</div>

{%endblock%}